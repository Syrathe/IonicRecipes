import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemyCourseOnIonicNg',
  appName: 'newApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
